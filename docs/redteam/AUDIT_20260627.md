# claude-harness self red-team — initial audit (2026-06-27)

Run by formation worker `claude-slate-falcon` per gh #27. Workflow: `plugins/harness-magi/workflows/harness_red_team.js` (7 Claude lenses → cross-family codex round → synthesis). Lens preset: `examples/harness_red_team_perspectives.md`.

**Read-only audit. All findings are proposals — mutation is human-gated.**

## Run stats

- reviewers returned: 7/7 ・ cross-family (codex) ok: yes ・ raw findings: 49 → 28 synthesized

- severity: CRITICAL 0 ・ HIGH 11 ・ MED 9 ・ LOW 7 ・ nit 1

- category: conflict 2 ・ stale 7 ・ dead 4 ・ hole 8 ・ noise 3 ・ residue 2 ・ injection 2

- cross-family-only catches (codex found, all Claude lenses missed): **5** — the gh #195 same-family-blind-spot value, realized.


## Summary

Harness health is mixed: the credential-leak defense-in-depth concept is sound but riddled with gaps, and the repo-vs-runtime split is the structural rot source. The dominant systemic defect — raised by all 7 Claude lenses and verified live — is that the 'canonical' claude-harness plugins are NOT installed (only frontend-design + security-guidance enabled), so every guard authored against the repo (temporal_anchor, branch_policy_guard, code_review_suggest, check_*) is dead, while CLAUDE.md:97 still advertises the temporal rail as an active structural backstop. The most dangerous finding is the autonomous mars PG rotation triggered by any DSN string in attacker-controllable tool output (poisoned file/WebFetch/MCP/mailbox) with no human gate — a remotely-inducible self-DoS of the ingest/bench pipeline. Codex earned its keep with 5 cross-family-only catches the same-family Claude lenses missed entirely: relative-.env guard bypass (which directly refutes a Claude finding that claimed grep .env was blocked), formation mailbox-body prompt injection, bypassPermissions spawning from arbitrary briefings, the HMAC scrubber fail-open on large outputs, and the non-expiring ACK bypass — concentrated in the formation spawn/messaging surface and the scrubber's failure modes, exactly the blind spot gh #195 predicted. Quick wins: fix the credential-leak count (5/9/24 → ≥10), de-duplicate the hetzner/talisker AgentShield bullet, and replace the banned decrypt-to-stdout|jq examples that 6 formation files still teach.


## Live corroboration

During issue-filing the `credential_value_scrub.sh` hook fired on the audit text itself (finding bodies *naming* credential patterns), re-triggering the autorotate followup (#26). No real secret was present — this is a real-time demonstration of finding #3 (benign content containing credential-shaped strings induces the autonomous rotation pipeline) and the over-firing class.


## Findings (full)

### [HIGH/dead] temporal_anchor.sh 'structural rail' cited in CLAUDE.md:97 is wired nowhere live

- **location**: `~/.claude/CLAUDE.md:97 vs ~/.claude/settings.json SessionStart + ~/.claude/hooks/`

- **sources**: rule-conflict, drift-stale, dead-rule, security-hole, over-firing, behavioral-residue, injection-surface (7 lens)

- **confidence**: high

- **rationale**: The §時系列hallucination防止 rule asserts a SessionStart hook injects absolute time coordinates (TODAY/project-age/last-commit/memory-mtime) as the structural backstop for a documented repeat failure class. Verified live: grep -c temporal_anchor settings.json = 0; ~/.claude/hooks/temporal_anchor.sh absent; enabledPlugins = only frontend-design + security-guidance (harness-core not installed). The file lives only in the un-enabled plugin. The rail never fires; only the behavioral 禁止語彙 list and the platform currentDate remain, so the agent self-certifies a structural protection it does not have. This is the single most-corroborated defect (7 of 7 lenses).

- **proposed fix (NOT applied)**: Copy temporal_anchor.sh into ~/.claude/hooks/ and add it to settings.json SessionStart (or enable harness-core). Until then, rewrite CLAUDE.md:97 to state the rail is NOT active and grounding is behavioral.


### [HIGH/stale] claude-harness plugins are not the deployed artifact — repo hooks.json drifts bidirectionally from live settings.json

- **location**: `~/projects/claude-harness/plugins/harness-*/hooks/hooks.json vs ~/.claude/settings.json + installed_plugins.json`

- **sources**: rule-conflict, drift-stale, dead-rule, behavioral-residue, injection-surface (5 lens)

- **confidence**: high

- **rationale**: The repo is presented as canonical but none of harness-core/magi/rails/formation are in enabledPlugins; the live agent runs hand-wired ~/.claude/hooks/. Divergence is bidirectional: repo wires branch_policy_guard, temporal_anchor, code_review_suggest (added in 16c5833), check_zsh_reserved_vars, check_early_check_timer — all ABSENT live; live wires long_task_advisor, self_check_reminder, vastai_create_followup_check, ghost_inject, recent_topics_inject — ABSENT from the plugin. Consequences: every guard committed to the 'canonical' repo ships dead (the code_review_suggest/check_* hooks are dead-on-arrival), AND enabling the plugins would silently drop the live-only memory-injection rails (ghost_inject/recent_topics have no plugin equivalent). Reviewers audit a fiction.

- **proposed fix (NOT applied)**: Pick one source of truth. Port ghost_inject/recent_topics into the plugin before any enablement flip, and add a CI check that diffs settings.json wired hooks against the plugins' hooks.json union, flagging orphans and dormant additions.


### [HIGH/dead] branch_policy_guard.sh — the repo-agnostic main-commit blocker — is not wired live

- **location**: `plugins/harness-core/hooks/branch_policy_guard.sh vs ~/.claude/settings.json PreToolUse Bash; CLAUDE.md:190`

- **sources**: rule-conflict, drift-stale, behavioral-residue (3 lens)

- **confidence**: high

- **rationale**: CLAUDE.md:190 makes main HEAD ≤ dev HEAD load-bearing and the guard exists specifically because per-repo .git/hooks/pre-commit 'per-repo install が要るため未配置 repo で素通り' (layer 1.5). Verified: grep -c branch_policy_guard settings.json = 0; it is absent from ~/.claude/hooks/. Only claude-harness's own untracked .git/hooks/pre-commit covers it; any other repo has zero agent-level main-commit block, collapsing to AI self-discipline — the exact mode behind the documented 11-commit direct-push incident.

- **proposed fix (NOT applied)**: Wire branch_policy_guard.sh into live PreToolUse Bash (copy to ~/.claude/hooks/ + settings.json) or enable harness-core.


### [HIGH/conflict] LIVE formation skill teaches/approves the canonical-banned decrypt-to-stdout | jq idiom

- **location**: `~/.claude/skills/formation SKILL.md:216 (live); plugins/harness-formation: mailbox.sh, redact.sh, bin/formation, briefing.md, README_ja.md`

- **sources**: security-hole, injection-surface (2 lens)

- **confidence**: high

- **rationale**: Global SOPS rule is absolute (only sops edit / sops exec-env; decrypt-to-stdout + outer pipe = hard violation, 9+ incident lineage). Yet the live formation SKILL.md:216 lists the decrypt-pipe-jq command marked ✓ APPROVED, and the credential-leak refuse message in mailbox.sh actively suggests the banned form at the worst possible moment. Verified: 6 formation plugin files still teach it. The canonical repo also lags the live njslyr7 fix (briefing.md still ships it). The skill that governs worker spawning coaches the exact leak-prone idiom behind the documented failure mode.

- **proposed fix (NOT applied)**: Replace every decrypt-to-stdout|jq example with sops exec-env <file> '<cmd>' across SKILL.md:216, mailbox.sh, redact.sh, bin/formation, briefing.md, README_ja.md; align with briefing.md's own ban statement.


### [HIGH/injection] Attacker-controlled tool output can trigger autonomous mars PG role rotation with no human gate

- **location**: `~/.claude/hooks/credential_value_scrub.sh:84-144 → autorotate_leaked_cred.sh`

- **sources**: security-hole (1 lens)

- **confidence**: med

- **rationale**: credential_value_scrub.sh parses the role out of any postgres(ql)://user:pass@ string in ANY Bash tool output, then fires autorotate_leaked_cred.sh DETACHED via setsid (verified: autorotate present, 3 setsid refs, DRYRUN default 0). If the role is a NON_PROD_ROLE it runs _rotate_mars_pg_roles.sh --execute against canonical mars and propagates to edges. The 'leak' need not be real: any content the agent merely reads (poisoned file, WebFetch page, MCP response, peer mailbox body surfaced in output) containing a 12-char DSN forces a real rotation = DoS of ingest/bench (in-flight jobs break) with zero confirmation. Lens confirmed the path runs live (dummy DSN spawned the followup, created claude-harness#26).

- **proposed fix (NOT applied)**: Require an explicit human/issue ack before any --execute rotation; never rotate from WebFetch/MCP-sourced output. Gate so rotation only proceeds when the leaked DSN also appears in a locally-authored file.


### [HIGH/hole] formation mailbox hard-refuse misses the project's primary secrets (PG DSN, POSTGRES/TURSO/R2)

- **location**: `plugins/harness-formation/lib/redact.sh:14-18 (is_credential_like)`

- **sources**: security-hole (1 lens)

- **confidence**: high

- **rationale**: CLAUDE.md promises mailbox_send hard-refuses credential-shaped bodies, and mailbox is permanent plaintext jsonl the user tails. But is_credential_like only matches a fixed provider-name list plus sk-/ghp_/AKIA/AIza/JWT/PEM. It does NOT cover postgresql://user:pass@ DSNs, POSTGRES_PASSWORD=, TURSO_AUTH_TOKEN=, R2_SECRET_ACCESS_KEY= — the project's most-used secrets (lens confirmed all four return PASSED). The PostToolUse scrub catches these, so a secret blocked from the transcript can still be written into the permanent mailbox visible to every peer pane — the 9+ incident leak class re-opened on the formation channel.

- **proposed fix (NOT applied)**: Make is_credential_like reuse the same DSN + KEYWORD=value catalog as credential_value_scrub.sh from a single shared pattern source so mailbox-refuse and transcript-scrub cannot drift.


### [HIGH/residue] 真田 silent-backup has fully enumerated objective triggers but remains behavioral — no PreToolUse backup hook

- **location**: `~/.claude/CLAUDE.md:45-52 vs ~/.claude/hooks/bash_command_guard.sh`

- **sources**: behavioral-residue (1 lens)

- **confidence**: high

- **rationale**: The 真田 protocol enumerates machine-checkable destructive triggers (rm -rf w/ wildcard, find -delete/-exec rm, DROP/TRUNCATE/DELETE-without-WHERE, git reset --hard/clean -fdx, > overwrite, .env/dotfile/DB edits, sed -i) and a deterministic backup path — textbook structural-hook material on a trigger surface a PreToolUse interceptor already covers. Yet no hook performs any backup; the only sanada reference (admission_reminder.sh) is a post-hoc UserPromptSubmit nudge firing AFTER breakage keywords appear. This directly violates CLAUDE.md:202 ('structural で書けないか先に問う'): an enumerable invariant left as model self-discipline, so a forgotten backup is silent and unrecoverable.

- **proposed fix (NOT applied)**: Add a PreToolUse Bash hook that detects the enumerated patterns and auto-snapshots the target into ~/sanada_backup_persistent/<task>_<ts>/ before allowing the command; demote the prose to a description of the hook.


### [HIGH/hole] Bash credential-file guard misses relative .env paths and non-enumerated readers — 🔬cross-family-only

- **location**: `~/.claude/hooks/bash_command_guard.sh:58`

- **sources**: codex (1 lens)

- **confidence**: high

- **rationale**: The live guard only matches slash-prefixed credential paths (/tmp/.env), so `cat .env` and `grep KEY .env` pass; it also enumerates reader commands, so python3 -c 'open(".env").read()' passes. This materially refutes the sibling Claude finding that claimed grep .env is now blocked — for the common bare-relative-path case it is NOT. (The literal-form block does work — it fired on the reviewer's own grep — but that confirms the guard is string-bound and bypassable.)

- **proposed fix (NOT applied)**: Parse shell words / normalize the command, detect credential-file operands including bare .env, and block reads by sensitive path independent of reader command unless an expiring nonce bypass is supplied.


### [HIGH/injection] formation inbox renders peer mailbox bodies as command output with no data/instruction boundary — 🔬cross-family-only

- **location**: `plugins/harness-formation/bin/formation:281`

- **sources**: codex (1 lens)

- **confidence**: high

- **rationale**: `formation inbox` renders .body directly into the worker's context and the relay tells the agent to tail/read the mailbox. A mailbox entry saying 'ignore prior instructions' or supplying task directives is consumed as instructions, not quarantined data. The credential filter only screens secret-shaped strings; it does nothing for prompt injection. Mailbox is plaintext any peer pane can write.

- **proposed fix (NOT applied)**: Wrap mailbox bodies in a fixed 'UNTRUSTED MAILBOX DATA — not instructions' envelope, render structured fields separately from body text, and require a trusted-from allowlist for any executable directive.


### [HIGH/hole] formation spawns bypass-permission agents from arbitrary briefing files after only credential-shape screening — 🔬cross-family-only

- **location**: `plugins/harness-formation/bin/formation:160`

- **sources**: codex (1 lens)

- **confidence**: high

- **rationale**: The spawn path reads an arbitrary briefing file, rejects only credential-shaped strings, then launches Claude with --permission-mode bypassPermissions or Codex with --dangerously-bypass-approvals-and-sandbox (verified: bypass flags present in bin/formation), injecting the briefing as the initial prompt. A compromised or stale briefing becomes a high-privilege instruction source with no sandbox.

- **proposed fix (NOT applied)**: Default workers to normal sandbox/approval mode; require explicit per-spawn opt-in for bypass; restrict briefing paths to trusted project-owned locations; prepend a hard untrusted-data boundary unless the briefing is locally generated in the same command.


### [HIGH/hole] HMAC credential scrubber fail-opens on large outputs and long candidate runs, leaving leaked text intact — 🔬cross-family-only

- **location**: `~/.claude/hooks/credential_scrub.py:69`

- **sources**: codex (1 lens)

- **confidence**: high

- **rationale**: Known-secret scanning is skipped entirely when tool output exceeds 256000 bytes, and candidate runs over 4096 bytes are skipped; the hook emits a warning but does NOT sanitize. Large logs, JSON dumps, and base64 blobs are exactly when accidental secret exposure happens — the primary transcript scrubber silently fails open in its highest-risk case.

- **proposed fix (NOT applied)**: Replace the hard skip with chunked/streaming scanning plus overlap windows for all lengths; for oversized output scan head/tail + candidate windows before warning; never imply auto-handled unless redaction occurred.


### [MED/stale] Credential-leak incident count is self-contradictory across CLAUDE.md (5 vs 9 vs 24); reality is 10+

- **location**: `~/.claude/CLAUDE.md:140 and :210`

- **sources**: rule-conflict, dead-rule (2 lens)

- **confidence**: high

- **rationale**: L140 says '9 回' and '24-incident 集約'; L210 (the distilled rail meant to fire every session) says '5 回目防止'; both point to memory feedback_credential_leak_5_incidents. Ground truth memory files include _9_ and _10_third_party_plaintext, so actual count ≥10. This is the corpus-size 5M-vs-10M archetype applied to the most safety-critical counter: a reader anchoring on L210 calibrates vigilance to a problem less than half as frequent as reality.

- **proposed fix (NOT applied)**: Pick one current number (≥10), update L140 and L210, and rename/re-point the memory or mark it a historical snapshot.


### [MED/stale] sops_edit_wrapper.sh is orphaned (wired nowhere) so hash-scrub manifests go stale after rotations

- **location**: `~/.claude/hooks/sops_edit_wrapper.sh + CLAUDE.md:146`

- **sources**: dead-rule, codex (2 lens)

- **confidence**: med

- **rationale**: The wrapper exists in ~/.claude/hooks/ (and the plugin) but is referenced by nothing live — not in settings.json, not in any hooks.json PreToolUse, and the live scrub does not invoke it. Meanwhile the global rule instructs plain `sops edit <file>`, which bypasses the manifest-refresh the wrapper was meant to perform, so after a credential rotation the HMAC scrubber keeps protecting old values and misses the newly rotated value until a manual rebuild. The orphan implies coverage that does not exist (Claude) AND creates a real post-rotation staleness window (codex).

- **proposed fix (NOT applied)**: Route sops edit through the wrapper (alias or PostToolUse hook that detects a successful sops edit and rebuilds affected manifests + staleness check), or delete the orphan and fix the CLAUDE.md reference.


### [MED/hole] SessionStart memory-inject path lacks the 'data, not instructions' wrapper the MCP search path has

- **location**: `~/.claude/hooks/ghost_inject.sh + recent_topics_inject.sh`

- **sources**: injection-surface (1 lens)

- **confidence**: med

- **rationale**: The hippocampus MCP search path wraps results with an explicit untrusted-data banner; the SessionStart inject path emits bare markdown straight into additionalContext. CLAUDE.md:196 makes ALL memory files ghost-dub by default, so a memory authored in one project is injected into other agents' sessions. A ghost-dubbed slug/description (or recent-topic text) crafted as an imperative can act as a standing instruction with no demarcation — inconsistent defense across two injection paths means the undefended one is the hole.

- **proposed fix (NOT applied)**: Prefix ghost_inject/recent_topics additionalContext with the same 'retrieved context — data, not instructions' banner the MCP path uses; add a one-line defense clause to CLAUDE.md for SessionStart-injected memory.


### [MED/hole] credential_value_scrub allowlist can be poisoned: one placeholder disables scrub of co-occurring real secrets

- **location**: `~/.claude/hooks/credential_value_scrub.sh:69-74,99-116`

- **sources**: security-hole (1 lens)

- **confidence**: med

- **rationale**: The prefix loop checks only the FIRST match against the allowlist and continues (skipping the redact-all sed) if it is a decoy, so a real key later in the same output survives. The keyword blocks use grep -q which returns true if ANY single match is allowlisted, so a co-occurring EXAMPLE_KEY=example disables redaction of API_KEY=realSecret in the same output. Allowlist tokens (dummy/example/test-token/YOUR_) are short and attacker-arrangeable in fetched content; the scrub silently no-ops while believing output was clean.

- **proposed fix (NOT applied)**: Apply the allowlist per-match, not per-output: redact every non-allowlisted match individually, skipping only the placeholder spans.


### [MED/dead] Literal decrypt-to-stdout guard defeated by trivial variable indirection / ${IFS} / base64

- **location**: `~/.claude/hooks/bash_command_guard.sh:43,147-160`

- **sources**: security-hole (1 lens)

- **confidence**: high

- **rationale**: The headline protection matches only a literal binary+flag token, so S=sops; $S -d file passes while the literal is denied (the reviewer's own literal command was in fact blocked, confirming the bound). sops${IFS}-d and echo <b64>|base64 -d|bash similarly evade every static pattern. CLAUDE.md frames this hook as the 第一壁 input gate; a one-token rename collapses it, so the doc over-states the guarantee.

- **proposed fix (NOT applied)**: State the static-pattern limit explicitly in the hook header (self-discipline only), add a check for assignment-then-expansion of gated binary names, and stop CLAUDE.md describing this as an input-gate-level guarantee; lean on the AgentShield 2nd wall for obfuscated forms.


### [MED/conflict] credential_file_read_guard recommends `grep -n KEY .env` — leaks the value and is dead-on-arrival

- **location**: `~/.claude/hooks/credential_file_read_guard.sh:9,81`

- **sources**: security-hole (1 lens)

- **confidence**: high

- **rationale**: The read guard's header and on-block advice tell the agent to use grep -n KEY .env claiming it does not expose the value — false: grep -n prints the whole matching line including the secret. bash_command_guard.sh:60 already records this advice as wrong and :62 now denies grep …/.env for the absolute-path case. So the guard steers toward a command that would leak if it ran and is blocked for absolute paths, offering no working path except the ack-bypass. (See also codex H8: for bare relative .env it is NOT blocked, so the leak actually executes.)

- **proposed fix (NOT applied)**: Rewrite lines 9/81 to point at sops exec-env <file> 'env | grep -c KEY' (count) / env | cut -d= -f1 (names), matching bash_command_guard's approved alternatives; drop the false no-exposure claim.


### [MED/noise] pipeline_preflight_gate hard-blocks parallel loops below its own stated N≥4 threshold — ⤳ dup of #5 (FP: pipeline_preflight_gate bulk-parallel-loop fires on numeric-range body text)

- **location**: `~/.claude/hooks/pipeline_preflight_gate.sh:74-83`

- **sources**: over-firing (1 lens)

- **confidence**: high

- **rationale**: The comment claims trigger #4 is for N≥4 background jobs, but the regex matches any $(seq ...) loop with a single backgrounded job regardless of count, e.g. for i in $(seq 2); do curl & done (2 jobs) trips a hard exit-2 block forcing a 30-min ack file. Legitimate low-N parallel work is blocked under the wolf-cry of a gate advertising N≥4, eroding trust and costing ack round-trips.

- **proposed fix (NOT applied)**: Tighten the regex to actually require N≥4 (count the seq bound / enumerated tokens or & occurrences), or downgrade this specific trigger from exit-2 to a warning.


### [MED/stale] Global CLAUDE.md feedback_*/user_* pointers do not resolve in the claude-harness memory namespace

- **location**: `~/.claude/CLAUDE.md (~20 citations) vs ~/.claude/projects/-home-hrmtz-projects-claude-harness/memory/`

- **sources**: behavioral-residue (1 lens)

- **confidence**: med

- **rationale**: The distilled rules cite memories (feedback_temporal_claims_require_grounding, feedback_credential_leak_5_incidents, feedback_harness_structural_primary, feedback_persona_stack) that physically exist only under OTHER project namespaces; the claude-harness memory dir holds only MEMORY.md + 3 files, none of them. While working in claude-harness a 'see → memory X' pointer has no local file; resolution depends entirely on cross-project ghost search succeeding — a latent broken-pointer surface if dub/search is down or a memory is renamed.

- **proposed fix (NOT applied)**: Mirror the load-bearing feedback_* memories into the claude-harness namespace, or change the citation convention to an explicit project+slug locator so the pointer is verifiable.


### [MED/hole] HRMTZ_ACK_CRED_READ 'one-time' credential-read bypass is neither one-time nor expiring — 🔬cross-family-only

- **location**: `~/.claude/hooks/credential_file_read_guard.sh:11`

- **sources**: codex (1 lens)

- **confidence**: high

- **rationale**: Comments promise a 1回 limit, but the Read guard accepts any process env with HRMTZ_ACK_CRED_READ=1 and the Bash guard accepts any command prefixed with it — no nonce, no state consumption, no TTL, no reason logging beyond best-effort. Once exported (or injected) the credential-read gate is permanently open for the session.

- **proposed fix (NOT applied)**: Replace the static env bypass with a tool that mints a single-use nonce file with TTL, consumes it on first matching read, and logs command/path/reason.


### [LOW/stale] AgentShield rule duplicated with stale host name (hetzner) and retired 'Discord webhook'

- **location**: `~/.claude/CLAUDE.md:180 vs :210`

- **sources**: rule-conflict, dead-rule, behavioral-residue (3 lens)

- **confidence**: high

- **rationale**: The 5-node AgentShield rule appears twice. L180 uses 'talisker', L210 uses '旧名 hetzner' for the same host (L114 declares talisker = 旧名 hetzner, a known confusion source); L210 also says 'Discord webhook' though L75-88 declares it retired 2026-05-25 in favor of discord-bot. Two near-identical rules drifting apart is exactly the rot that makes a reader trust the wrong copy.

- **proposed fix (NOT applied)**: Collapse the two bullets into one, use 'talisker' and 'discord-bot post'.


### [LOW/stale] harness-core hook-count self-description drifts (README '3'/'4', hooks.json '9', actual ~12)

- **location**: `plugins/harness-core/README.md:3,84 vs hooks.json description`

- **sources**: rule-conflict, drift-stale (2 lens)

- **confidence**: high

- **rationale**: README L3 says 'Three hooks' and L84 tells installers to copy four scripts, while hooks.json describes '9 hooks' and actually wires ~12. Anyone installing by the README copies 4 and gets a fraction of the advertised defense-in-depth (credential_scrub PostToolUse etc. silently omitted); the at-a-glance inventory readers trust is wrong and will re-drift on the next addition.

- **proposed fix (NOT applied)**: Enumerate the full current hook+copy set matching hooks.json, and replace the numeric count with a derived/non-numeric phrasing so it stops drifting.


### [LOW/dead] formation_suggest.sh runs on every prompt but is permanently shadow-mode and could not emit valid hook JSON even if activated

- **location**: `plugins/harness-formation/hooks/formation_suggest.sh:18-21,40-45`

- **sources**: over-firing, codex (2 lens)

- **confidence**: med

- **rationale**: The hook defaults MODE=shadow and only injects njslyr7 when FORMATION_SUGGEST_MODE=active; no override exists anywhere, so it is always shadow — a subprocess on every UserPromptSubmit with zero possible signal (its 'switch after 24h' comment is long stale). Worse, codex notes that even in active mode it echoes bare 'njslyr7' rather than the hookSpecificOutput.additionalContext JSON envelope, so the documented active path is non-functional too.

- **proposed fix (NOT applied)**: Emit the standard hook JSON envelope via jq matching sibling UserPromptSubmit hooks, add a smoke test for active mode, then either flip to active after reviewing the shadow log or unwire it.


### [LOW/stale] CLAUDE.md cites scripts/install_branch_hooks.sh as the branch-hook install path — script does not exist

- **location**: `~/.claude/CLAUDE.md §Workflow (Branch policy)`

- **sources**: drift-stale (1 lens)

- **confidence**: med

- **rationale**: The branch policy rule names 'scripts/install_branch_hooks.sh 経路'; grep/find over claude-harness returns nothing (the script exists only in other repos), and there is no committed git/hooks/pre-commit template. A reader deploying the branch guard to a new repo finds a dead pointer.

- **proposed fix (NOT applied)**: Create scripts/install_branch_hooks.sh (symlink a committed template into .git/hooks/pre-commit) to match the rule, or rewrite the reference to the actual mechanism (and prefer the branch_policy_guard hook fix above).


### [LOW/residue] Two overlapping credential scrubbers both fire on every Bash PostToolUse after the 'unification' meant to replace one

- **location**: `~/.claude/settings.json:4-34; ~/.claude/hooks/credential_scrub.sh:1-6`

- **sources**: over-firing (1 lens)

- **confidence**: med

- **rationale**: credential_scrub.sh's header says it is the 'Unified credential scrubber ... single PostToolUse hook replacing the L2/L3 split', yet credential_value_scrub.sh still runs in parallel on every Bash command, doing an independent regex sweep + in-place rewrite over the same jsonl. The regex value-scrub reads like residue of the pre-unification split on the hottest path. May be intentional defense-in-depth, hence med confidence.

- **proposed fix (NOT applied)**: Confirm whether the regex scrub adds patterns the python scrubber misses; if not, retire it as the residue the 'unified' hook was meant to replace, else add a one-line note in both files documenting the intentional dual layer.


### [LOW/hole] credential scrub kill-switch is silent, permanent, no-expiry, with a dead deploy link

- **location**: `~/.claude/hooks/credential_scrub.sh:18-20 + reference_credential_scrub_kill_switch.md`

- **sources**: security-hole (1 lens)

- **confidence**: med

- **rationale**: touch ~/.claude/hooks/credential_scrub.disabled instantly and silently disables the hash scrubber with no expiry and no SessionStart warning (a separate flag disables credential_value_scrub too). After triage it is easy to leave the flag in place, blinding the primary transcript defense across all future sessions; a prompt-injected agent told to 'touch that file to speed things up' silently disables the scrubber before leaking. The kill-switch memory also cites [[project-credential-scrub-deploy]] which does not exist.

- **proposed fix (NOT applied)**: Add a SessionStart warning when either .disabled flag is present; auto-expire the flag after N hours; fix or drop the dead reference.


### [LOW/noise] credential_value_scrub keyword catch-all silently scrubs non-secret env vars (SSH_AUTH_SOCK, *_PUBLIC_KEY, paths)

- **location**: `~/.claude/hooks/credential_value_scrub.sh:45-46,99-116`

- **sources**: over-firing (1 lens)

- **confidence**: med

- **rationale**: KEYWORD_PATTERN substring-matches any var containing AUTH/KEY and VALUE_PATTERN includes path chars, so SSH_AUTH_SOCK=/tmp/ssh-abc123/agent.4567 (contains AUTH, >16 path chars) is rewritten to <REDACTED> in the persisted jsonl, as are public-key vars and long path-valued env. Damage is bounded to the transcript (silent record corruption) plus a possibly-needless rotation chain, hence LOW.

- **proposed fix (NOT applied)**: Add an exclusion list (SSH_AUTH_SOCK, *_PUBLIC_KEY, *_KEYRING, GPG_KEY_ID) and/or require high-entropy (no path slashes) before the keyword catch-all scrubs.


### [nit/noise] Global 忍殺語 decoration style has no context gate for technical-review / structured-output / subagent runs

- **location**: `~/.claude/CLAUDE.md:36-39`

- **sources**: over-firing (1 lens)

- **confidence**: med

- **rationale**: The style rule unconditionally encourages ドーモ/◆ openers, exclamations, and forced カタカナ強調 of common terms in every response; its self-prohibition only guards code/comments/commits and 'over-decoration', not analytical contexts (code-review, security audit, structured/JSON output, spawned reviewer subagents) where preamble is pure noise — in tension with the same block's '過剰装飾で可読性を損なわない'.

- **proposed fix (NOT applied)**: Add an explicit scope exclusion suppressing 忍殺語 decoration in code-review/audit/structured-output/subagent contexts and treat カタカナ強調 as optional, not default.



## Cadence (APPROVED 2026-06-27 — user 中村さん via cinder-wren %32)

| trigger | when | lenses |
|---|---|---|
| periodic full sweep | monthly | 5: rule-conflict, drift-stale, dead-rule, security-hole, injection-surface (+ codex cross-family) |
| change-driven diff audit | on harness edit / pre-push | 3: rule-conflict, security-hole, behavioral-residue (+ codex) |

Always cross-family (codex). Read-only; `--fix` forbidden; mutation human-gated.

**cron wiring: DEFERRED** — host + schedule undecided (topology: which host runs it is TBD).
Cadence is recorded here and in #27; no cron is installed yet. Re-run manually meanwhile via
`Workflow({scriptPath:"plugins/harness-magi/workflows/harness_red_team.js"})`.


## #40 — formation SoT consolidation (DONE 2026-06-27)

SoT decision (user 中村さん): **claude-harness/plugins/harness-formation** is canonical.
Grounded: plugin is a strict superset of njslyr7 (commit `cec2a36` merged njslyr7 + added
session_id / --new-window / spawn-UUID; newer mtime on every file; `comm` showed no
njslyr7-only files). The only live consumer of the stale njslyr7 copy was the skill symlink.

Executed:
- `~/.claude/skills/formation` repointed njslyr7 → plugin/skills/formation (CLI already → plugin).
  Both live entrypoints now resolve under the single plugin SoT; the wiring split is closed.
- njslyr7 formation copies marked DEPRECATED (markers in skills/bin/lib; not deleted).
- Verified: symlink resolves, SKILL.md frontmatter valid, templates present, `formation` CLI smoke OK,
  and the harness re-injected the skill description from the new target = clean fresh load.
- Rollback: `~/sanada_backup_persistent/formation_sot_consolidate_*/symlinks.before`.

Live symlinks still pointing at njslyr7: 0.

## #40 + #41 final state (2026-06-27, "plugin 優先")

User decision: **plugin (claude-harness/plugins/harness-formation) is the sole SoT; njslyr7 is fully part of claude-harness.**

- **#40 fully complete**: njslyr7's formation copies (bin/formation, lib/mailbox.sh,
  lib/mailbox_relay.sh, skills/formation) are now **symlinks → the plugin**. Both the
  live skill symlink and njslyr7 resolve to the same plugin tree; the duplicate is
  gone, so bidirectional drift is structurally impossible. (真田 backup:
  njslyr7_formation_retire_*.) njslyr7 repo now shows these as symlinks (uncommitted
  there — separate repo; user's call to commit/gitignore).
- **#41 (B)→(A)**: the earlier "keep v1 live, v2 dev-only" choice is **superseded** —
  it was structurally incompatible with plugin-as-SoT (sync deploys all hooks). With
  plugin as SoT, the codex-APPROVED v2 source-trust autorotate gate is the live version
  (untrusted→hard-block; auto only for bare local pg_dump; ambiguous→human-ack), and
  the #45 v2-backend repoint rides with it. plugin == live, consistent.
